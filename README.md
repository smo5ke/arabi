# عربي (Arabi) - لغة برمجة عربية

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Rust](https://img.shields.io/badge/Rust-1.70+-orange.svg)](https://www.rust-lang.org)
[![Tests](https://img.shields.io/badge/tests-136%20passed-brightgreen.svg)]()

لغة برمجة عربية كاملة ببنية Python-like و اداء C++-competitive.

## الميزات الرئيسية

- **بنية عربية بالكامل**: متغيرات، دوال، اصناف، استيراد — كلها باسماء عربية
- **39 كلمة مفتاحية**: جميع ميزات Python تقريباً
- **اداء عالي**: VM مبني بـ Rust مع تحسينات متقدمة
- **170+ دالة مدمجة**: رياضيات، نصوص، وقت، عشوائي، ملفات، JSON، نظام
- **نظام اصناف كامل**: وراثة، طرق، magic methods، decorators
- **مطابقة الانماط**: `طابق`/`حالة` مع دعم القيم والنصوص
- **وصول ديناميكي**: `خاصية()`، `تعيين_خاصية()`، `هل_خاصية()`
- **التغليف (Closures)**: دوال داخلية مع `محلي` و `عام`
- **معالجة اخطاء**: `حاول`/`خلل`/`اخيرا` مع استثناءات مخصصة وارقام اسطر
- **مدير سياق**: `باستخدام` مع `__ادخل__`/`__اترك__`
- **استيراد و حزم**: 6 وحدات مدمجة + ملفات + نظام حزم
- **المولّدات**: `yield`، `yield from`، `ابعث()` (send)
- **CLI متقدم**: REPL تفاعلي + ادارة حزم + الوان

## المتطلبات

- [Rust](https://www.rust-lang.org/tools/install) 1.70 او احدث

## التثبيت

```bash
git clone https://github.com/smo5ke/arabi.git
cd arabi
cargo build --release
```

## الاستخدام

```bash
# تشغيل ملف
arabi ملفي.txt

# REPL تفاعلي
arabi

# وضع التصحيح (يعرض bytecode)
arabi -d ملفي.txt

# اوامر الحزم
arabi pakg create حزمة_جديدة    # انشاء حزمة
arabi pakg list                  # عرض الحزم
arabi pakg info حزمة             # معلومات حزمة
arabi pakg remove حزمة           # حذف حزمة
arabi pakg update حزمة           # تحديث حزمة
```

## نظام الحزم

```bash
# انشاء حزمة جديدة
arabi pakg create حسب

# هذا ينشئ:
#   __pakgs__/حسب/__beginning__.txt
#   __pakgs__/حسب/pakg.json

# استيراد الحزمة في الكود
# استورد حسب
```

## REPL التفاعلي

```bash
# تشغيل REPL
arabi

# اوامر REPL
/مساعدة        # عرض المساعدة
/خروج          # الخروج
/حالة          # عرض المتغيرات

# امثلة في REPL
عربي> س = 10
عربي> اطبع(س + 5)
15
```

## امثلة سريعة

### المتغيرات والعمليات
```
س = 10
ص = "مرحبا"
اطبع(س + 5)        # 15
اطبع(س ^ 2)        # 100
اطبع("مرحبا " + "عالم")  # مرحبا عالم
```

### الدوال
```
دالة جمع(ا، ب):
    ارجع ا + ب

اطبع(جمع(3، 4))    # 7

# *args و ^kwargs
دالة مختلطة(ا، *الاضافي، ^الكثرة):
    اطبع("ا:", ا، "اضافي:", الاضافي، "كثرة:", الكثرة)
```

### الاصناف
```
صنف مخروط:
    دالة __تهيئة__(هذا، نق = 1):
        هذا.نق = نق

    دالة مساحة(هذا):
        ارجع هذا.نق ^ 2 * 3.14

م = مخروط(5)
اطبع(م.مساحة())    # 78.5...
```

### المولّدات
```
دالة عداد(حد):
    س = 0
    بينما س < حد:
        سلم(س)
        س += 1

لكل ع في عداد(5):
    اطبع(ع)    # 0, 1, 2, 3, 4
```

### معالجة الاخطاء
```
حاول:
    س = 10 / 0
خلل قسمة_بصفر:
    اطبع("خطا!")
نهاية:
    اطبع("تنظيف")
```

### تركيبات
```
مربعات = [س ^ 2 لكل س في مدى(6)]
اطبع(مربعات)    # [0, 1, 4, 9, 16, 25]

ف = {س: س ^ 2 لكل س في مدى(5)}
اطبع(ف)        # {0: 0, 1: 1, 2: 4, 3: 9, 4: 16}
```

## الكلمات المفتاحية (39)

| الفئة | الكلمات |
|-------|---------|
| التحكم | `اذا`، `اواذا`، `والا`، `بينما`، `لكل`، `في`، `توقف`، `استمر`، `مرور` |
| الدوال | `دالة`، `ارجع`، `خطية`، `سلم`، `سلّم` |
| الاصناف | `صنف`، `هذا`، `اصل` |
| الاستيراد | `استورد`، `من`، `بشرط` |
| الاستثناءات | `حاول`، `خلل`، `نهاية`، `ارم` |
| النطاق | `عام`، `محلي`، `باستخدام` |
| المقارنات | `يساوي`، `يساوي ليس` |
| طابق | `طابق`، `حالة`، `حالة_اخرى` |
| اخرى | `احذف`، `اكد`، `و`، `او`، `ليس`، `صح`، `خطا`، `عدم` |

## العمليات

| النوع | الرموز |
|-------|--------|
| حسابية | `+` `-` `*` `/` `\` `%` `^` |
| مقارنة | `==` `!=` `<` `>` `<=` `>=` |
| منطق | `و` `او` `ليس` |
| اسناد | `=` `+=` `-=` `*=` `/=` `\=` `%=` `^=` `:=` |

## المكتبات القياسية

| المكتبة | الاستيراد | الوصف |
|---------|----------|-------|
| `حساب` | `استورد حساب` | sin, cos, tan, sqrt, pow, pi, e, ... |
| `وقت` | `استورد وقت` | time, sleep, now, date, ... |
| `نص` | `استورد نص` | len, replace, split, find, ... |
| `عشوائي` | `استورد عشوائي` | rand, rand_int, seed, ... |
| `كائن` | `استورد كائن` | تحليل, تحويل (JSON) |
| `نظام` | `استورد نظام` | ادارة, قائمة_ادارة, وجود, ... |

## Magic Methods

| الطريقة | الاستخدام |
|---------|----------|
| `__تهيئة__` | مُنشئ الصنف (init) |
| `__نص__` | تحويل لنص (str) |
| `__عرض__` | تمثيل رسمي (repr) |
| `__استدعاء__` | استدعاء الكائن كدالة |
| `__طول__` | طول الكائن (len) |
| `__اجمع__` / `__اجمع_ع__` | عملية الجمع (+) |
| `__ادخل__` / `__اترك__` | مدير السياق (with) |
| `__بادئ__` | بادئ التكرار (iter) |

## الاداء

متوسط الاداء: **2.44x اسرع** من Python (15 اختبار — 13 فوز، 2 خسارة)

| الاختبار | عربي | Python | النسبة |
|---------|------|--------|--------|
| Arithmetic (10M) | 58.3ms | 75.9ms | **1.30x اسرع** |
| Strings (100K concat) | 9.8ms | 93.5ms | **9.58x اسرع** |
| Lists (100K append) | 9.9ms | 23.4ms | **2.37x اسرع** |
| Nested Loops (1M) | 9.9ms | 30.2ms | **3.04x اسرع** |
| Fibonacci (recursive 35) | 51.7ms | 70.0ms | **1.35x اسرع** |
| Closures (100K) | 15.2ms | 24.2ms | **1.59x اسرع** |
| Comprehension (100K) | 14.7ms | 21.3ms | **1.45x اسرع** |
| Classes (100K) | 26.7ms | 29.8ms | **1.12x اسرع** |
| F-Strings (100K) | 17.8ms | 24.1ms | **1.36x اسرع** |
| Exceptions (100K) | 9.0ms | 21.2ms | **2.36x اسرع** |
| Math (sin/cos 1M) | 21.1ms | 33.8ms | **1.60x اسرع** |
| Dicts (100K) | 10.3ms | 37.9ms | **3.68x اسرع** |
| Factorial Recursion | 44.6ms | 43.3ms | 0.97x |
| Bubble Sort | 10.2ms | 40.0ms | **3.91x اسرع** |
| String Processing | 25.3ms | 22.8ms | 0.90x |

### تحسينات الاداء المطبقة

| التحسين | الوصف |
|---------|-------|
| **JIT Compilation** | Cranelift JIT للفيبوناتشي والحلقات |
| **String Interning** | بحث O(1) للاسماء بدلاً من O(n) |
| **Peephole Optimizer** | 30+ تمرير تحسين bytecode |
| **Fused Opcodes** | دمج العمليات في opcode واحد |
| **INT_INT Specialization** | تخصيص عمليات الاعداد الصحيحة |
| **FastMethod Dispatch** | تمرير مباشر للطرق البسيطة |
| **Unsafe Field Access** | تخطي RefCell للقراءات |
| **Inline Fields** | تخزين حتى 4 حقول داخل الكائن |
| **mimalloc** | خاضع الذاكرة عالي الاداء |
| **Instance-first Fast Path** | مسار سريع لانشاء الكائنات |
| **Stability Hardening** | 0 unwrap() خطرة في كود الانتاج |

## البنية التقنية

| المكون | التقنية | السطور |
|--------|---------|--------|
| Lexer | Hand-written tokenizer + Unicode Arabic | ~492 |
| Parser | Recursive Descent | ~1305 |
| Compiler | AST → Bytecode + 30+ Peephole Passes | ~3366 |
| VM | Stack-based + Fused opcodes + JIT | ~5005 |
| JIT | Cranelift-based integer JIT | ~617 |
| Builtins | 170+ دالة مدمجة | ~2400 |
| CLI | REPL + Package manager + Colored output | ~315 |
| Tests | 136 integration tests | |

## امتداد الملف

`.txt` او `.عربي`

## ملاحظات تقنية

- التعليقات تبدا بـ `#`
- فاصلة عربية `،` بين المعاملات
- فاصلة انجليزية `,` في تعريف الدوال والقوائم
- الارقام العربية والانجليزية مدعومة
- لا يوجد hamza (ا) في اي كلمة مفتاحية

## التوثيق

- [BUILTINS.md](docs/BUILTINS.md) — الدوال المدمجة (170+)
- [USAGE.md](docs/USAGE.md) — دليل استخدام اللغة
- [STDLIB.md](docs/STDLIB.md) — المكتبات القياسية
- [CHANGELOG.md](CHANGELOG.md) — سجل التغييرات
- [COMPARISON.md](benchmarks/COMPARISON.md) — مقارنة الاداء مع Python
- [PROJECT_MAP.md](PROJECT_MAP.md) — بنية اللغة الداخلية والهيكل الكامل

## الترخيص

[MIT License](LICENSE) — رخصة مفتوحة المصدر
