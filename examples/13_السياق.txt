# 13 - مدير السياق (with statement)
# ملاحظة: requires a file that already exists
استورد نظام
اذا نظام.وجود("ملفي.txt"):
    باستخدام افتح("ملفي.txt") بشرط ملف:
        محتوى = ملف.اقرأ()
        اطبع(محتوى)
والا:
    اطبع("الملف غير موجود - جرب تشغيل المثال 12 أولاً")
